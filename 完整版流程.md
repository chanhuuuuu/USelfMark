

# USelfMark 完整下载&本地安装教程
## 一、从 GitHub 下载源码（两种方式）
### 方式1：新手推荐，下载 ZIP 压缩包
1. 打开项目仓库地址：`https://github.com/chanhuuuuu/USelfMark`
2. 页面右上角点击绿色按钮 `Code`
3. 在下拉菜单选择 `Download ZIP`，浏览器自动下载源码压缩包
4. 找到下载好的 `USelfMark-main.zip`，**完整解压到纯英文无空格路径**
    - ❌ 错误示例：`/文档/USelf Mark 工具`（含中文/空格）
    - ✅ 正确示例：`/Users/xxx/tools/USelfMark`
5. 打开解压文件夹，确认根目录存在 `manifest.json` 文件

### 方式2：有Git环境，命令行克隆（开发者使用）
打开终端执行命令：
```bash
git clone https://github.com/chanhuuuuu/USelfMark.git
```
执行完成后，本地生成 `USelfMark` 源码文件夹

---

## 二、浏览器加载扩展（Chrome / Edge 通用，Windows & Mac 操作一致）
1. 打开浏览器扩展管理页面
    - Chrome：地址栏输入 `chrome://extensions/` 回车
    - Edge：地址栏输入 `edge://extensions/` 回车
2. 打开页面右上角的 **开发者模式** 开关
3. 点击页面左上角「加载已解压的扩展程序」
4. 在弹窗中选中**解压后的 USelfMark 根文件夹**（能直接看到 manifest.json 的目录）
5. 确认选择，扩展安装完成，浏览器右上角工具栏出现 `USelfMark` 图标

---

## 三、前置 Ollama 跨域配置（必须操作，否则扩展报错）
### Windows 用户
1. 进入 USelfMark 文件夹，双击 `Windows启动Ollama.bat`
2. 黑色命令窗口保持后台最小化，关闭窗口服务停止
3. 验证服务：新开 CMD 输入
```cmd
curl http://127.0.0.1:11434/api/tags
```
返回模型列表即代表跨域生效

### Mac 用户
1. 终端进入项目目录，执行配置脚本
```zsh
sh Mac-Ollama环境配置.sh
```
2. 点击屏幕顶部 Ollama 图标 → `Quit Ollama` 完全退出
3. 重新打开 Ollama App，跨域配置自动生效

### 通用模型拉取命令
```bash
# 图文识别（推荐，支持网页截图解析）
ollama pull llava:latest
# 纯文本轻量化总结
ollama pull gemma3:latest
```

---

## 四、Notion 配置与扩展初始化
1. 进入 Notion，创建集成，复制以 `secret_` 开头的 Token
2. 新建数据库，字段名称、类型严格匹配：
    | 字段名称 | 字段类型 |
    | ---- | ---- |
    | 标题 | 标题 |
    | 链接 | URL |
    | 分类 | 多选标签 |
    | 摘要 | 富文本 |
3. 复制数据库ID（数据库页面右上角「开发者」菜单获取）
4. 点击浏览器右上角 `USelfMark` 图标 → 点击 `⚙️ 设置参数`
5. 依次填入：模型名称、Notion Token、Database ID
6. 点击「保存并返回」，即可正常使用一键归档功能

---

## 五、使用验证流程
1. 打开任意网页（小红书、博客、资讯页均可）
2. 点击扩展图标，按下 `🚀 智能归档到 Notion`
3. 等待自动抓取页面、截图、AI生成摘要标签
4. 提示「🎉图文完整归档成功！」即全部流程部署完成

---

## ⚠️ 常见安装避坑
- 解压路径不能包含中文、空格、特殊符号，否则浏览器加载失败
- Ollama 必须开启跨域配置，否则扩展会出现跨域拦截报错
- Notion 数据库字段名称一字不能修改，否则写入API报错
- 仅支持 `http/https` 网页，`chrome://` 系统内置页面无法截图解析
